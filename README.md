# 🌀 Torslate (Translator & OCR Application)

**Torslate** คือเครื่องมือแปลภาษาบนหน้าจออัจฉริยะที่ช่วยให้คุณเข้าถึงเนื้อหาภาษาต่างประเทศได้ทันที ไม่ว่าจะเป็นในเกม, วิดีโอที่ไม่มีซับไตเติล หรือเอกสารที่ไม่สามารถคัดลอกข้อความได้ โดยใช้พลังของ **Google Cloud API**

---

## 🚀 ฟีเจอร์เด่น (Key Features)

### 1. 🔍 Normal Mode (Capture & Translate)
* **Hotkey:** `Ctrl + Alt + T`
* ลากพื้นที่ที่ต้องการบนหน้าจอ เพื่อสแกนข้อความ (OCR) และแปลภาษาทันทีในรูปแบบ Overlay

### 2. 🎮 Story Mode (Fixed Region)
* **Set Region (`Ctrl + Alt + R`):** ลากเพื่อกำหนดขอบเขตพื้นที่คงที่ (เช่น กล่องคำพูดในเกม) จะมีกรอบสีขาวบางๆ แสดงตำแหน่งไว้ตลอดเวลา
* **Instant Translate (`Ctrl + Alt + E`):** กดเพื่อแปลภาษาจากพื้นที่ที่ตั้งค่าไว้ทันทีด้วยความรวดเร็ว โดยไม่ต้องลากใหม่

### 3. 🖼️ Subtitle Style Overlay
* แสดงคำแปลกึ่งกลางหน้าจอในรูปแบบซับไตเติล
* สามารถลากย้ายตำแหน่งหน้าต่างผลลัพธ์ได้อิสระ และระบบจะ **จำตำแหน่งล่าสุด** ไว้เสมอ
* มีปุ่มสลับดูข้อความต้นฉบับ (Original) และคำแปล (Translated) ได้อย่างง่ายดาย

### 4. ⌨️ Manual Translate Window
* หน้าต่างสำหรับพิมพ์หรือวางข้อความเพื่อแปลเองโดยเฉพาะ พร้อมระบบคัดลอกข้อความในคลิกเดียว

---

## 🛠️ การตั้งค่าก่อนใช้งาน (Setup)

### 1. ความต้องการของระบบ
* **Python 3.10+**
* **Google Cloud Project** ที่เปิดใช้งาน:
    * Cloud Vision API
    * Cloud Translation API

### 2. การติดตั้ง Library
* **เปิด Terminal หรือ CMD แล้วพิมพ์คำสั่งดังนี้:**
    * pip install PyQt6 pynput mss google-cloud-vision google-cloud-translate google-auth

### 3. การจัดการ API Key
* **นำไฟล์ Service Account Key (JSON) มาจาก Google Cloud Console**

* **เปลี่ยนชื่อไฟล์เป็น key.json**

* **นำไปวางไว้ในโฟลเดอร์เดียวกับไฟล์ main_app.py**

## 📂 โครงสร้างโปรเจกต์ (Project Structure)
* **main_app.py: ไฟล์หลักสำหรับรันโปรแกรมและจัดการหน้าต่าง UI ทั้งหมด**

* **cloud_processor.py: ส่วนประมวลผลการเชื่อมต่อกับ Google Cloud (OCR & Translation)**

* **hotkey_listener.py: ส่วนดักจับการกดปุ่มคีย์ลัดจากคีย์บอร์ด**

* **key.json: ไฟล์กุญแจสำคัญสำหรับเข้าใช้งาน Google Cloud API**

## 💰 ข้อมูลค่าบริการและโควตา (Pricing & Quotas)
* **โปรแกรมนี้ทำงานภายใต้ Free Tier ของ Google Cloud ซึ่งมีข้อจำกัดดังนี้:**

* **Google Translation: ฟรี 500,000 อักขระต่อเดือน (เครดิตมูลค่า $10)**

* **Google Vision (OCR): ฟรี 1,000 ครั้งแรกต่อเดือน**

* **สามารถตรวจสอบปริมาณการใช้งานแบบ Real-time ได้ที่ Google Cloud Console**

## 🏗️ การสร้างไฟล์สำหรับใช้งาน (.exe)
* **หากต้องการแปลงเป็นไฟล์ .exe สำหรับ Windows ให้ใช้คำสั่ง:**
    * pyinstaller --noconsole --onefile --icon="icon.ico" --name="Torslate" main_app.py
---
**หมายเหตุ: เมื่อสร้างเสร็จ ต้องวางไฟล์ key.json ไว้คู่กับไฟล์ Torslate.exe เสมอ**


## 🔑 วิธีดาวน์โหลดไฟล์ `key.json` (Google Cloud Service Account Key)

ไฟล์ `key.json` คือกุญแจสำคัญที่ใช้ยืนยันตัวตนเพื่อให้โปรแกรมของคุณสามารถเรียกใช้ Google Vision API และ Translation API ได้

### 1. ไปที่ Google Cloud Console
คลิกที่ลิงก์นี้เพื่อเข้าสู่หน้าจัดการ Service Accounts:
👉 **[Google Cloud Console - Service Accounts](https://console.cloud.google.com/iam-admin/serviceaccounts)**

### 2. เลือก Project
* ตรวจสอบที่มุมบนซ้ายของหน้าเว็บ ว่าคุณได้เลือก **Project** ที่คุณเปิดใช้งาน API ไว้ถูกต้องแล้ว

### 3. สร้าง Service Account (หากยังไม่มี)
1. คลิกปุ่ม **"+ CREATE SERVICE ACCOUNT"** (หรือ "+ สร้างบัญชีบริการ") ด้านบน
2. **Service account name:** ตั้งชื่ออะไรก็ได้ (เช่น `torslate-app`)
3. กด **CREATE AND CONTINUE**
4. **Grant access to project (Role):** เลือกเป็น **Basic** > **Owner** (เพื่อให้มั่นใจว่าโปรแกรมมีสิทธิ์เข้าถึงทุก API)
5. กด **DONE**

### 4. สร้าง Key
1. ในตารางรายชื่อ Service Accounts ให้คลิกที่ **Email** ของบัญชีที่คุณเพิ่งสร้าง (ลิงก์สีน้ำเงิน)
2. คลิกที่แถบเมนู **KEYS** ด้านบน
3. คลิกปุ่ม **ADD KEY** > เลือก **Create new key**
4. เลือกประเภทไฟล์เป็น **JSON**
5. กดปุ่ม **CREATE**

### 5. ติดตั้งลงในโปรเจกต์
1. ไฟล์จะถูกดาวน์โหลดลงเครื่องคอมพิวเตอร์ของคุณอัตโนมัติ (ชื่อไฟล์จะยาวมาก)
2. เปลี่ยนชื่อไฟล์นั้นเป็น **`key.json`**
3. **สำคัญ:** นำไฟล์ไปวางไว้ในโฟลเดอร์เดียวกับไฟล์ `main_app.py` (หรือวางคู่กับไฟล์ `.exe` หากใช้งานแบบโปรแกรมสำเร็จรูป)
